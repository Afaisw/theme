{
  "name": "theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx pushstate-server -d server -p 3000",
    "watch": "npx json-server --watch data/db.json --port 8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "json-server": "^0.17.4",
    "pushstate-server": "^3.0.1",
    "readline-sync": "^1.4.10"
  }
}
